---
{
  "group": "features",
  "slug": "share-link",
  "title": "分享链接与权限",
  "description": "每条链接单独设密码、到期时间和打开次数上限，到期自动失效；谁进来过有记录，客户还能顺着同一条链接把原片回传。",
  "h1": "带密码和有效期的审片链接",
  "updatedOn": "2026-09-20",
  "sections": ["密码与有效期", "一次性验证码", "谁打开过有记录", "客户顺着链接回传原片", "微信里打开的效果"],
  "cta": { "href": "/login", "label": "登录后建一个团队，传一条片子试" },
  "related": ["features/frame-comments", "features/versions"],
  "faq": [
    { "q": "一条链接能只给客户看其中一条片子吗？", "a": "能。链接的范围可以设到整个项目、某个文件夹、单条片子，甚至这一条片子的某一个版本，权限再按条单独勾。" },
    { "q": "改密码或延长期限，要重新建一条链接吗？", "a": "不用。密码、到期时间、打开次数上限、权限项、有效还是失效，都是这条链接上的字段，改完立即生效，已有的访问次数和意见仍挂在原来那条片子上。" },
    { "q": "客户没收到验证码邮件怎么办？", "a": "查两件事：他的邮箱有没有被登记成这个项目的收件人，以及站点有没有配好发信通道。两种原因页面都不会报错，回给你的那句话不暴露名单，所以别当成 bug。这时候改用链接密码那条路。" },
    { "q": "把链接撤销之后，客户已经提的意见会丢吗？", "a": "不会。意见记在对应片子的对应版本上，跟链接不是同一层的东西；把链接置为失效、甚至删掉这条分享记录，那条片子上的意见还在。" },
    { "q": "客户要看片，得先注册吗？", "a": "不用注册账号，拿到链接并过了密码这一关就能看和留言，前提是这条链接勾了留言权限。只有一种情况例外：他从“以访客身份继续”那个入口进来，那条路给的是只读权限，能看不能写；而这个入口只对团队级地址开放，单条链接分享碰不到它。" }
  ]
}
---

## 密码与有效期

每条链接单独带三样东西：密码、到期时间、打开次数上限。密码可以手填，也可以让它自动生成 4 位数字；到期时间是任意一个日期时刻；次数上限就是“打开够 N 次就停”。这三个字段长在链接身上，不在账号身上——给 A 客户那条设成周五过期，跟给 B 客户那条互不相干。

默认值要看清：创建时不勾密码，链接就是免密直进；到期时间和次数上限两格不填，就没有到期日，也不会因为被人打开过几次而关掉。这套东西没有替你设保守默认，发出去之前把那三格扫一眼。

权限同样按条勾：看、留言、下载、点通过，四样各开各的，留言那一栏的能力在[逐帧批注](/features/frame-comments)那页写细；审阅分享、交付分享、收录分享是三类不同的链接，各配一套。每条链接还能出一张二维码。要收回来随时置为失效，散出去的老地址立刻只剩一个打不开的页面，片子本身一点不动。

## 一次性验证码

这是项目一级的开关，不是每条链接的选项。开起来之后，客户在分享页填自己的邮箱，收到一封带 6 位验证码的邮件，填对才进得去；一个码只用一次，验过就作废，10 分钟内有效。它是邮件验证码，不是短信。

两处边界。一处是收信人：码只会发给已经被登记成这个项目收件人的邮箱，不在名单里的人填什么都不会真的收到，页面回的那句提示也不告诉你有没有这个人；这类取码尝试会写进安全事件。另一处是通道：发信走你自己配置的邮件通道，通道没通的时候，用链接密码那条路更省事。密码和验证码的失败次数都有阈值，超了会临时锁住。

## 谁打开过有记录

每一次进入留一条记录：什么时间、走哪道门进来的（密码、邮箱验证码、访客、免密），走验证码那条路的还留下他填的邮箱。项目这一级的分析里能看到总访问、按进入方式分的计数、独立访客数。

隐私这条线说清楚一点：对方的 IP 和浏览器信息只在他同意分析之后才留存，不同意就做匿名化处理，整套分析也可以关掉。所以这份记录回答的是“什么时候、以什么方式被打开过”，它不是一份“来看片的人都是谁”的名单。

## 客户顺着链接回传原片

反向这条路叫“收录”。管理员开启后生成一条独立的收录链接，客户或外包顺着它把原始素材、参考文件、交付文件传上来。上传走分片，网络断了接着传；配了对象存储的，浏览器直传那条路也支持。单个文件的上限由管理员设置，默认 1 GB，可以往上调；一次会话能传几个也是设的，默认 10 个，上限能调到 500。

收回来的文件不会顶掉你的片子：它先停在收录的收件箱里，你看过之后手动选“覆盖为新版本”，它才变成那条片子的下一版，[旧版本连同上传者和时间一起留着](/features/versions)。交付型链接不提供上传入口，收和发是分开的两条路。

## 微信里打开的效果

就是一串普通的 https 地址：粘在微信里发出去，对方在聊天窗口点开了就是页面本身；也可以把那条链接的二维码发出去，他用相机扫。没有做微信内的专门适配——不能拿微信授权进审片页，也不会生成微信里的分享卡片，视频在微信内置浏览器里的实际表现我们没在真机上逐条验过。

对方也没有客户端要装：没有 iOS 和 Android 应用，能加到主屏的就是这个网页。所以你在微信里发片子的动作不用改，还是发一串地址；变的是发出去之后——到期自动关，次数用完自动关，你随时手动让它失效，不用再追着补一句“上面那条链接别看”。
