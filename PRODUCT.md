# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

「逐帧审阅」的团队管理员 —— 短剧/分账内容方（生产上真实团队：剧爆影视）。`/studio/projects` 是他们每天打开的第一屏。

正在做的两件事：判断每一部剧现在走到哪一步，以及把这个状态直接讲给客户或自己团队听（用户点名：打开首页的第一任务就是「给客户或团队看进度」）。

## Product Purpose

影视后期与客户批注的协作后台：项目 = 一部剧，里面是按集组织的视频版本，客户在画面上逐帧批注。首页成功 = 管理员不点进项目就能说清每部剧的进度与卡点。

## Positioning

批注精确到帧、版本可锁帧对比播放。通用网盘、IM 传文件、或普通协作工具给不了这个精度。

## Operating Context

- 一个团队 = 一家内容方，顶部团队切换器；团队有到期日（生产显示「11 天后到期」）。
- 项目有编号（001 / 004 / 005），单项目视频量级 19–181 个（集数），批注 8–312 条。
- 状态只有四个：审片中 / 已通过 / 仅分享 / 已归档。
- 2026-09-23 生产只读取证：4 个项目全部「审片中」，客户名、截止日期、描述、收件人**全部为空**；100% 的视频有 `thumbnailPath`。

## Capabilities and Constraints

- 保留的功能（用户点名）：搜索、筛选（状态/客户/年份/到期）、分组（状态/客户）、批量管理、收藏视图、置顶、归档折叠。页面结构可以推倒重来，功能清单不行。
- 规模预期：单团队 50 个项目上下。不做虚拟列表那一套，但要能一眼扫完。
- 首页主信息是进度、版本数、交付状态，不是附属统计数字。
- 封面帧不是免费的：缩略图要逐视频签发令牌（`/api/studio/video-token`）。首页要用真实帧，需要新增批量签发接口。
- 视觉体系沿用现有 token（浅色/深色/薄荷等多主题），单个页面不得自造一套身份。中文为主，zh/en/de/nl 四语都要过。

## Brand Commitments

「逐帧审阅」名称与 LogoMark；多主题 token 体系（含尚未上线的薄荷浅色主题）。

## Evidence on Hand

- 真实项目数据 4 条：黑手党、月圆之吻-分账项目、TheAlphaWolf'sCaptiveHiddenPup、测试项目（勿动）。
- 两版既有实现：线上旧版（4 张光卡）、工作区未上线版（`src/components/projects/ProjectsDashboard.tsx` 等）。两版都被用户否掉，作为反参考使用。
- 缺失且不得编造：客户名、截止日期、项目描述、收件人、客户 logo、证言、评分。

## Product Principles

1. 首页的任务是讲清进度，不是列出项目。
2. 真实数据里为空的字段，不能成为主视觉的支柱。
3. 画面（视频帧）是这个产品唯一独有的素材，优先于任何抽象图标。
4. 50 个项目靠排序与分组收敛，不靠折叠藏起来。
5. 页面要能直接截图发给客户讲进度。
